services:
  backend:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - hf_cache:/root/.cache/huggingface
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  hf_cache:
